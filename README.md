# BandVZ
## Prerequisites
* PostgreSQL 15.18
* Node.js 24.10.0
* NPM 11.6.1
## Local installation
* Clone the repository
* Run `npm install`
* Run `npx tsoa routes`
* Run `cp .env.sample .env`
* Edit `.env` and change important configuration
* Run `npm run dev`
