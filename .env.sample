# The port the API server should listen on
PORT=3000

# A secret for JWTs
JWT_SECRET=123456789

# PostgreSQL database credentials
DB_HOST=localhost
DB_PORT=5432
DB_USER=bandvz
DB_PASSWORD=
DB_NAME=bandvz
